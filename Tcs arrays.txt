result=0;int place=1;                                              
result=result*10+digit;                                                 
result=digit*place+result;                       
place*=10;
num=num*10+(str[i]-'0');



1.second highest nd second smallest



#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    int min=arr[0],max=arr[0],sh=-10000,ss=10000;
    for(int i=0;i<n;i++)
    {
        if(max<arr[i])
        {
        sh=max;
        max=arr[i];
        }
        else if(arr[i]>sh && arr[i]<max)
        sh=arr[i];
        if(arr[i]<min)
        {
        ss=min;
        min=arr[i];
        }
        else if(arr[i]<ss && arr[i]>min)
        ss=arr[i];
    }
    printf("%d %d ",sh,ss);
    
}


2.reverse  a given array


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
     for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
    int i=0,j=n-1;
    while(i<j)
    {
        int temp=arr[i];
        arr[i]=arr[j];
        arr[j]=temp;
        j--;
        i++;
    }
    
    for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
}



count frequency of each element in the array


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
     for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
    for(int i=0;i<n;i++)
    {
        int count=0;
        int alreadycount=0;
        for(int k=0;k<i;k++)
        {
            if(arr[i]==arr[k])
            {
            alreadycount=1;
            break;
            }
        }
        if(alreadycount)
        {
            continue;
        }
        for(int j=0;j<n;j++)
        {
            if(arr[i]==arr[j])
            count++;
        }
        printf("value:%d is count:%d",arr[i],count);
        printf("\n");
    }
}


first half elements increasing order nd nd other half elements decresing order 


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
     for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
   int k=n/2;
   for(int i=0;i<k-1;i++)
   {
       for(int j=0;j<k-i-1;j++)
       {
           if(arr[j]>arr[j+1])
           {
               int temp=arr[j+1];
               arr[j+1]=arr[j];
               arr[j]=temp;
           }
       }
   }
   for(int i=k;i<n-1;i++)
   {
       for(int j=k;j<n-(i-k)-1;j++)
       {
           if(arr[j]<arr[j+1])
           {
               int temp=arr[j+1];
               arr[j+1]=arr[j];
               arr[j]=temp;
           }
       }
   }
   for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
}


Rearrange array in increasing-decreasing order



0

Problem Statement: Rearrange a given array such that the first half
is arranged in increasing order,
 and the second half is arranged in decreasing order


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
     for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
    int k=n/2;
   for(int i=0;i<n-1;i++)
   {
       for(int j=0;j<n-i-1;j++)
       {
           if(arr[j]>arr[j+1])
           {
               int temp=arr[j+1];
               arr[j+1]=arr[j];
               arr[j]=temp;
           }
       }
   }
   for(int i=k;i<n-1;i++)
   {
       for(int j=k;j<n-(i-k)-1;j++)
       {
           if(arr[j]<arr[j+1])
           {
               int temp=arr[j+1];
               arr[j+1]=arr[j];
               arr[j]=temp;
           }
       }
   }
   for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
}


sum of array elements


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
     for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
    int result=0;
   for(int i=0;i<n;i++)
   {
       result+=arr[i];
   }
   printf("%d",result);
}



📝 Quick Cheat Sheet (Save this)
Statement in question	Choose
“Shift left”	LEFT
“Shift right”	RIGHT
“Clockwise”	RIGHT
“Anti-clockwise”	LEFT
“First elements to end”	LEFT
“Last elements to front”	RIGHT


#include<stdio.h>
#include<string.h>
int main()
{
    int n,k;
     char str[20];
    scanf("%s",str);
    scanf("%d",&n);
    scanf("%d",&k);
    int arr[100];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
   for(int i=0;i<n;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n");
    int i=0;
    if(strcmp(str,"left")==0)
    {
        while(i<k)
        {
            int temp=arr[0];
            for(int l=0;l<n-1;l++)
            {
                arr[l]=arr[l+1];
            }
            arr[n-1]=temp;
            i++;
        }
        
        
    }
    else if(strcmp(str,"right")==0)
    {
       while(i<k)
        {
            int temp=arr[n-1];
            for(int l=n-1;l>0;l--)
            {
                arr[l]=arr[l-1];
            }
            arr[0]=temp;
            i++;
        } 
    
       
    }
    for(int i=0;i<n;i++)
    {
        printf("%d",arr[i]);
    }
    
}



Average of all the elements in the array


#include<stdio.h>
#include<string.h>
int main()
{
    int n,result=0;
    float k;
    
    scanf("%d",&n);
    
    int arr[100];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<n;i++)
    {
        result+=arr[i];
    }
   if(result%n==0)
   {
       printf("%d",result/n);
   }else{
       printf("%.1f",(float)result/n);
   }
}


Find Median of the given Array


#include<stdio.h>
int main()
{
    int n,result=0;
    float k;
    
    scanf("%d",&n);
    
    int arr[100];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<n-1;i++)
    {
        for(int j=0;j<n-i-1;j++)
        {
            if(arr[j]>arr[j+1])
            {
                int temp=arr[j];
                arr[j]=arr[j+1];
                arr[j+1]=temp;
            }
        }
    }
    if(n%2==1)
    {
        printf("%d",arr[n/2]);
    }
    else{
        int sum=arr[n/2]+arr[n/2-1];
        if(sum%2==0)
        printf("%d",sum/2);
        else
        printf("%.1f",(float)sum/2.0);
    }
    
}


Remove Duplicates in-place from Sorted Array


#include<stdio.h>
int main()
{
    int n,result=0;
    scanf("%d",&n);
    int arr[100];
    for(int i=0;i<n;i++)
    {
        if(i==0)
        scanf("%d",&arr[i]);
        else{
            int x;
            scanf("%d",&x);
            if(x>=arr[i-1])
            {
               arr[i]=x; 
            }
        }
            
    }
    for(int i=0;i<n;i++)
    {
        printf("%d", arr[i]);
    }
    printf("\n");
    int k=0;
    arr[k]=arr[0];
    k=1;
    for(int i=1;i<n;i++)
    {
       if(arr[i]!=arr[i-1])
       {
           arr[k]=arr[i];
           k++;
       }
    }
    for(int i=0;i<k;i++)
    {
        printf("%d", arr[i]);
    }
}


Remove Duplicates From an Unsorted Array


#include<stdio.h>
int main()
{
    int n,result=0;
    scanf("%d",&n);
    int arr[100];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
            
    }
    for(int i=0;i<n;i++)
    {
        printf("%d", arr[i]);
    }
    printf("\n");
   
    for(int i=0;i<n;i++)
    {
        int duplicate=0;
        for(int j=0;j<i;j++)
        {
            if(arr[i]==arr[j])
            {
                duplicate=1;
                break;
            }
        }
        if(!duplicate)
        printf("%d",arr[i]);
    }

}


Adding Element in an Array



1

Problem Statement: Given an array of N integers, write a program to add an array element at the beginning, end, and at a specific position.


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    int beg,end,value,pos;
    scanf("%d",&beg);
    scanf("%d",&end);
    scanf("%d",&value);
    scanf("%d",&pos);
    for(int i=0;i<n;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n");
    
    
    for(int i=n;i>0;i--)
    {
        arr[i]=arr[i-1];
    }
    arr[0]=beg;
    n++;
    arr[n]=end;
    n++;
    
    for(int i=n;i>=pos;i--)
    {
        arr[i]=arr[i-1];
    }
    arr[pos-1]=value;
    n++;
    printf("final array");
    for(int i=0;i<n;i++)
    {
        printf("%d",arr[i]);
    }
}


Find all repeating elements in an array

#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    
    for(int i=0;i<n;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n");
    for(int i=0;i<n;i++)
    {
        int count=0;
        int alreadycount=0;
        for(int k=0;k<i;k++)
        {
            if(arr[k]==arr[i])
            {
            alreadycount=1;
            break;
            }
        }
        if(alreadycount)
        continue;
        for(int j=0;j<n;j++)
        {
            if(arr[i]==arr[j])
            count++;
        }
        if(count>1)
        printf("%d",arr[i]);
    }
}


Find all the non-repeating elements in an array


#include<stdio.h>
int main()
{
    int arr[100];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    
    for(int i=0;i<n;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n");
    for(int i=0;i<n;i++)
    {
        int count=0;
       
        for(int j=0;j<n;j++)
        {
            if(arr[i]==arr[j])
            count++;
        }
        if(count==1)
        printf("%d",arr[i]);
    }
}


Find all Symmetric Pairs in the array of pairs


#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int first[100],second[100];
    for(int i=0;i<n;i++)
    {
        scanf("%d %d",&first[i],&second[i]);
    }
    int k=0;
    int res1[100],res2[100];
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<i;j++)
        {
            if(first[i]==second[j]&&second[i]==first[j])
            {
                res1[k]=first[i];
                res2[k]=second[i];
                k++;
            }
            
        }
    }
    for(int i=0;i<k-1;i++)
    {
        for(int j=0;j<k-i-1;j++)
        {
            if(res1[j]>res1[j+1])
            {
                int temp=res1[j];res1[j]=res1[j+1];res1[j+1]=temp;
                 temp=res2[j];res2[j]=res2[j+1];res2[j+1]=temp;
            }
        }
    }
    for(int i=0;i<k;i++)
    {
        printf("(%d, %d)",res1[i],res2[i]);
    }
}


Maximum Product Subarray in an Array

#include<stdio.h>
int max(int a,int b)
{
    return (a>b)?a:b;
}
int main()
{
    int n;
    int arr[100];
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    int prefix=1;
    int suffix=1;
    int result=arr[0];
    for(int i=0;i<n;i++)
    {
        prefix*=arr[i];
        result=max(result,prefix);
        if(prefix==0)
        prefix=1;
        suffix*=arr[n-i-1];
        result=max(result,suffix);
        if(suffix==0)
        suffix=1;
    }
    printf("%d",result);
    
}



#include<stdio.h>
int main()
{
    int n;
    int arr[100];
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    int prefix=1;
    int suffix=1;
    int maxproduct=arr[0];
    int start=0,end=0,pTempstart=0,sTempend=n-1;
    for(int i=0;i<n;i++)
    {
        prefix*=arr[i];
        if(prefix>maxproduct)
        {
            maxproduct=prefix;
            start=pTempstart;
            end=i;
        }
        if(prefix==0)
        {
        prefix=1;
        pTempstart=i+1;
        }
        suffix*=arr[n-i-1];
        if(suffix>maxproduct)
        {
            maxproduct=suffix;
            start=n-i-1;;
            end=sTempend;
        }
        if(suffix==0)
        {
        suffix=1;
        sTempend=n-i-2;
        }
    }
    printf("product:%d",maxproduct);
     printf("\n");
    printf("subarray is");
   
    for(int i=start;i<=end;i++)
    {
        printf("%d ",arr[i]);
    }
    return 0;
    
}


Replace elements by its rank in the array


#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[100],temp[100],rank[100];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
        temp[i]=arr[i];
    }
    for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
    for(int i=0;i<n-1;i++)
    {
        for(int j=0;j<n-i-1;j++)
        {
            if(temp[j]>temp[j+1])
            {
                int k=temp[j];
                temp[j]=temp[j+1];
                temp[j+1]=k;
            }
        }
    }
    int r=1;
    rank[0]=1;
    for(int i=1;i<n;i++)
    {
        if(temp[i]!=temp[i-1])
        r++;
        rank[i]=r;
    }
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            if(arr[i]==temp[j])
            {
                arr[i]=rank[j];
                break;
            }
        }
    }
    for(int i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
    
}

Sort Elements of an Array by Frequency



#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[100];
    int used[100]={0};
    for(int i=0;i<n;i++)
    scanf("%d",&arr[i]);
    for(int steps=0;steps<n;steps++)
    {
        int maxFreq=0;
        int index=-1;
        for(int i=0;i<n;i++)
        {
            if(used[i])
            continue;
            int count=0;
        for(int j=0;j<n;j++)
        {
            if(arr[i]==arr[j])
            count++;
        }
        if(count>maxFreq||(count==maxFreq&&(index==-1||arr[i]<arr[index])))
        {
            maxFreq=count;
            index=i;
        }
        
    }
    if(index==-1)
    break;
    for(int i=0;i<maxFreq;i++)
    printf("%d ",arr[index]);
    for(int i=0;i<n;i++)
    {
        if(arr[i]==arr[index])
        {
            used[i]=1;
        }
    }
    }
}



Finding Equilibrium index in an array


#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[100];
    for(int i=0;i<n;i++)
    scanf("%d",&arr[i]);
    int totalsum=0;
    for(int i=0;i<n;i++)
    totalsum+=arr[i];
    int leftsum=0;
    for(int i=0;i<n;i++)
    {
        int rightsum=totalsum-leftsum-arr[i];
        if(rightsum==leftsum)
        {
            printf("%d ",i);
            return 0;
        }
        leftsum+=arr[i];
    }
    printf("-1");
}



Sort an array according to the order defined by another array

#include<stdio.h>
int main()
{
    int n1,n2;
    scanf("%d",&n1);
    scanf("%d",&n2);
    int arr1[100],arr2[100],freq[1000]={0};
    for(int i=0;i<n1;i++)
    {
    scanf("%d",&arr1[i]);
    freq[arr1[i]]++;
    }
    for(int i=0;i<n2;i++)
    scanf("%d",&arr2[i]);
    for(int i=0;i<n2;i++)
    {
        while(freq[arr2[i]]>0)
        {
            printf("%d ",arr2[i]);
            freq[arr2[i]]--;
        }
    }
    for(int i=0;i<1000;i++)
    {
        while(freq[i]>0)
        {
            printf("%d ",i);
            freq[i]--;
        }
    }
}

Search an Element in an Array 

#include<stdio.h>
int main()
{
    int n1,k;
    int arr1[100];
    scanf("%d",&n1);
   scanf("%d",&k);
   
    for(int i=0;i<n1;i++)
    {
    scanf("%d",&arr1[i]);
    }
    
    for(int i=0;i<n1;i++)
    {
        if(arr1[i]==k)
        {
            printf("%d",i);
        }
    }
}


Check if a number is Palindrome or Not

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int temp=num;
    int result=0;
    while(num)
    {
        int digit=num%10;
        result=result*10+digit;
        num/=10;
    }
    if(result==temp)
    printf("polindrome");
    else
    printf("not polindrome");
}


Find all Palindrome Numbers in a given range

#include<stdio.h>
int main()
{
    int max,min;
    scanf("%d",&max);
    scanf("%d",&min);
    for(int i=min;i<=max;i++)
    {
    int temp=i;
    int num=i;
    int result=0;
    while(num)
    {
        int digit=num%10;
        result=result*10+digit;
        num/=10;
    }
    if(result==temp)
    printf("%d ",temp);

    }
}



Prime Numbers in a given range


#include<stdio.h>
int isprime(int value)
{
    if(value<2)
    return 0;
    for(int i=2;i*i<=value;i++)
    {
        if(value%i==0)
        return 0;
    }
    return 1;
}
int main()
{
    int min,max;
    scanf("%d %d",&min,&max);
    for(int i=min;i<=max;i++)
    {
        if(isprime(i))
        {
            printf("%d ",i);
        }
    }
}


Check if a number is Armstrong Number or not


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int temp=num;
    int count=0;
    while(temp)
    {
       count++;
       temp/=10;
    }
    temp=num;
    int sum=0;
    while(temp)
    {
        int digit=temp%10;
        int product=1;
        int tempcount=count;
        while(tempcount)
        {
            product*=digit;
            tempcount--;
        }
        sum+=product;
        temp/=10;
    }
    if(num==sum)
    printf("armstrong");
    else
    printf("not armstrong");
}



Check whether a number is Perfect Number or not


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int temp=num;
    int sum=0;
    for(int i=1;i<=num/2;i++)
    {
        if(num%i==0)
        sum+=i;
    }
    if(sum==num)
    printf("perfect number");
    else
    printf("not perfect number");
}

perfect number in a range 

#include<stdio.h>
int main()
{
    int max,min;
    scanf("%d  %d",&min,&max);
  for(int i=min;i<=max;i++)
  {
      int temp=i;
      int sum=0;
    for(int i=1;i<=temp/2;i++)
    {
        if(temp%i==0)
        sum+=i;
    }
    if(sum==temp)
    printf("%d ",temp);
  }
}


Check whether a given number is even or odd


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    if(num%2==0)
    printf("even number");
    else
    printf("odd number");
}


Sum of first N Natural Numbers


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int sum=0;
    for(int i=1;i<=num;i++)
    sum+=i;
    printf("%d",sum);
}

print that ap series nd sum also sum

#include<stdio.h>
int main()
{
    int a,d,n;
    scanf("%d %d %d",&a,&d,&n);
    int sum=0;
    int term=a;
    for(int i=0;i<n;i++)
    {
        printf("%d ",term);
        sum+=term;
        term+=d;
    }
    printf("\n");
    printf("%d",sum);
}


Program to find Sum of GP Series


#include<stdio.h>
int main()
{
    int a,n;
    float r;
    scanf("%d %f %d",&a,&r,&n);
    float sum=0;
    float term=a;
    for(int i=0;i<n;i++)
    {
        printf("%.2f ",term);
        sum+=term;
        term*=r;
    }
    printf("\n");
    printf("%.2f",sum);
}

Check if given year is a leap year or not

#include<stdio.h>
int main()
{
    int year;
    scanf("%d",&year);
    if((year%4==0&&year%100!=0)||(year%400==0))
    printf("%d leap year",year);
    else
    printf("%d not leap year",year);
}


Reverse digits of a number


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int result=0;
    while(num)
    {
        int digit=num%10;
        result=result*10+digit;
        num/=10;
    }
    printf("reverse %d",result);
}


Maximum and Minimum Digit in a Number


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int max=0,min=9;
    while(num)
    {
        int digit=num%10;
        if(digit>max)
        max=digit;
        if(digit<min)
        min=digit;
        num/=10;
    }
    printf("max:%d min:%d",max,min);
}

Print Fibonacci Series up to Nth term

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int a=0,b=1;
    int i=0;
    while(i<=num)
    {
        printf("%d ",a);
        int next=a+b;
        a=b;
        b=next;
        i++;
    }
}

within range

#include<stdio.h>
int main()
{
    int min,max;
    scanf("%d %d",&min,&max);
    int a=0,b=1;
    while(a<=max)
    {
        if(a>=min)
        printf("%d ",a);
        int next=a+b;
        a=b;
        b=next;
        
    }
}

Factorial of a Number : Iterative and Recursive


#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int product=1;
    for(int i=num;i>0;i--)
    {
        product*=i;
    }
    printf("%d",product);
}

#include<stdio.h>
int fact(int value)
{
    if(value==0)
    return 1;
    return value*fact(value-1);
}
int main()
{
    int num;
    scanf("%d",&num);
    int result=fact(num);
    printf("%d",result);
}

Calculate the Power of a Number : Binary Exponentiation

#include<stdio.h>
#include<math.h>
int main()
{
    int base,exponent;
    scanf("%d %d",&base,&exponent);
    if(base==0&&exponent<0)
    {
        printf("undefined\n");
        return 0;
    }
    double result=1;
    int exp=exponent>0?exponent:-exponent;
    for(int i=0;i<exp;i++)
    result*=base;
    if(exponent<0)
    result=1.0/result;
    if(floor(result)==result)
    printf("%.0lf\n",result);
    else
    printf("%lf",result);
}

Factors of a Given Number

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    for(int i=1;i<=num;i++)
    {
        if(num%i==0)
        printf("%d ",i);
    }
}

Print all Prime Factors of the given number

#include<stdio.h>
int main()
{
   int n;
   scanf("%d",&n);
   for(int i=2;i*i<=n;i++)
   {
       if(n%i==0)
       {
           printf("%d ",i);
           while(n%i==0)
           n/=i;
       }
   }
   if(n>1)
   printf("%d",n);
}


Check if a number is a Strong Number or not


#include<stdio.h>
int factorial(int n)
{
    int result=1;
    for(int i=n;i>0;i--)
    {
        result*=i;
    }
    return result;
}
int main()
{
   int n;
   scanf("%d",&n);
   int temp=n;
   int sum=0;
   while(temp)
   {
       int digit=temp%10;
       sum+=factorial(digit);
       temp/=10;
   }
   if(sum==n)
   printf("strong number");
   else
   printf("not a strong number");
}

Check if a number is Automorphic Number

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int temp=num;
    int count=0;
    while(temp)
    {
        temp/=10;
        count++;
    }
    int power=1;
    for(int i=0;i<count;i++)
    power*=10;
    int result=num*num;
    int lastdigits=result%power;
    if(lastdigits==num)
    printf("automorphic number");
    else
    printf("not automorphic number");
}

Find GCD of two numbers


#include<stdio.h>
int gcd(int a, int b)
{
    while(b!=0)
    {
        int temp=b;
        b=a%b;
        a=temp;
    }
    return a;
}
int main()
{
    int n1,n2;
    scanf("%d %d",&n1,&n2);
    int result=gcd(n1,n2);
    printf("%d",result);
}

Find LCM of two numbers

#include<stdio.h>
int gcd(int a,int b)
{
    while(b!=0)
    {
        int temp=b;
        b=a%b;
        a=temp;
    }
    return a;
}
int main()
{
    int n1,n2;
    scanf("%d %d",&n1,&n2);
    int result;
    result=n1*n2/gcd(n1,n2);
    printf("%d",result);
}

GCD for more than two numbers


#include<stdio.h>
int gcd(int a,int b)
{
    while(b!=0)
    {
        int temp=b;
        b=a%b;
        a=temp;
    }
    return a;
}
int main()
{
    int n;
    int arr[100];
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    scanf("%d",&arr[i]);
    int result=arr[0];
    for(int i=0;i<n;i++)
    result=gcd(result,arr[i]);
    printf("%d",result);
}

lcd for more than two numbers

#include<stdio.h>
int gcd(int a,int b)
{
    while(b!=0)
    {
        int temp=b;
        b=a%b;
        a=temp;
    }
    return a;
}
int lcm(int a,int b)
{
    return (a*b)/gcd(a,b);
}
int main()
{
    int n;
    int arr[100];
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    scanf("%d",&arr[i]);
    int result=arr[0];
    for(int i=0;i<n;i++)
    result=lcm(result,arr[i]);
    printf("%d",result);
}


Check if the given number is Harshad(Or Niven) Number

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int temp=num;
    int sum=0;
    while(temp)
    {
        int digit=temp%10;
        sum+=digit;
        temp/=10;
    }
    if(num%sum==0)
    printf("Harshad number");
    else
    printf("not harshad number");
}

Check if the number is an abundant number or not

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    int sum=0;
    for(int i=1;i<=num/2;i++)
    {
        if(num%i==0)
        sum+=i;
    }
    if(sum>num)
    printf("abundant num");
    else
    printf("not abundant num");
}

Sum Of Digits of A Number

#include<stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    while(num>=10)
    {
        int sum=0;
        while(num)
        {
            sum+=num%10;
            num/=10;
        }
        num=sum;
    }
    printf("%d",num);
}


Find the sum of numbers in the given range

#include<stdio.h>
int main()
{
    int l,r;
    scanf("%d %d",&l,&r);
    int sum=0;
    for(int i=l;i<=r;i++)
    {
        sum+=i;
    }
    printf("%d",sum);
}

Permutations in which N people can occupy R seats

#include<stdio.h>
int fact(int N)
{
    int power=1;
    for(int i=N;i>=1;i--)
    power*=i;
}
int main()
{
    int N,r;
    scanf("%d %d",&N,&r);
    int result=fact(N)/fact(N-r);
    printf("%d",result);
}

Program to Add two fractions

#include<stdio.h>
int gcd(int a,int b)
{
    while(b!=0)
    {
        int temp=b;
        b=a%b;
        a=temp;
    }
    return a;
}
int main()
{
    int n1,d1,n2,d2;
    scanf("%d %d %d %d",&n1,&d1,&n2,&d2);
    int num=n1*d2+n2*d1;
    int deno=d1*d2;
    int g=gcd(num,deno);
    num/=g;
    deno/=g;
    printf("%d /%d",num,deno);
}


C program to add more than two fractions (N fractions)


#include<stdio.h>
int gcd(int a,int b)
{
    while(b!=0)
    {
        int temp=b;
        b=a%b;
        a=temp;
    }
    return a;
}
int main()
{
    int n;
    scanf("%d",&n);
    int num=0,den=1;
    for(int i=0;i<n;i++)
    {
        int n1,d1;
        scanf("%d %d",&n1,&d1);
        num=num*d1+n1*den;
        den=d1*den;
        int g=gcd(num,den);
        num/=g;
        den/=g;
    }
    printf("%d/%d",num,den);
}


Replace all the 0’s with 1 in a given integer


#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    if(n==0)
    {
        printf("1");
        return 0;
    }
    int result=0;
    int place=1;
    while(n)
    {
        int digit=n%10;
        if(digit==0)
        digit=1;
        result=digit*place+result;
        place*=10;
        n/=10;
    }
    printf("%d",result);
}


Express given number as Sum of Two Prime Numbers

#include<stdio.h>
int check(int value)
{
    if(value<2)
    return 0;
    for(int i=2;i*i<=value/2;i++)
    {
        if(value%i==0)
        return 0;
    }
    return 1;
}
int main()
{
    int n;
    scanf("%d",&n);
    int found=0;
    for(int i=2;i<=n/2;i++)
    {
        if(check(i)&&check(n-i))
        {
            printf("True\n");
            printf("%d+%d=%d",i,n-i,n);
            found=1;
            break;
        }
    }
    if(!found)
    printf("false");
    
}

Calculate the Area of the Circle

#include<stdio.h>
#define pi 3.14
int main()
{
    int r;
   
    scanf("%d",&r);
    float area=r*pi*r;
    printf("area:%.2f",area);
}


Program to Find Roots of a quadratic equation

#include<stdio.h>
#include<math.h>

int main()
{
    int a,b,c;
    float d,root1,root2,real,img;
    scanf("%d %d %d",&a,&b,&c);
    d=b*b-4*a*c;
    if(d>0)
    {
        root1=(-b+sqrt(d))/(2*a);
        root2=(-b-sqrt(d))/(2*a);
        printf("roots are real and different, i.e.(%.0f,%.0f)",root1,root2);
    }
    else if(d==0)
    {
        root1=-b/(2.0*a);
        printf("roots are rel and same,i.e(%.2f,%.2f)",root1,root2);
    }
    else
    {
        real=-b/(2.0*a);
        img=sqrt(-d)/(2.0*a);
        printf("roots are complex,i.e-(%.1f+i%.3f, %.1f+i%.3f)",real,img,real,img);
    }
}





